global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: "tasks_service"
    static_configs:
      - targets: ["localhost:8090"]

  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]
